package i.g.sbl.sky.repo.system;

import i.g.sbl.sky.entity.system.DictType;
import org.springframework.data.repository.CrudRepository;

public interface DictTypeRepo extends CrudRepository<DictType, Long> {
}
